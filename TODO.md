# TODO

- [x] Update `vite.config.js` to add `server.allowedHosts` for `letterless-unvicarious-rosaline.ngrok-free.dev` (and allow `*.ngrok-free.dev` if desired).

- [ ] Restart the Vite dev server and verify the ngrok host is accepted.

